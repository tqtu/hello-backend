version: '3.8'

services:
  hello-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hello-backend-app
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx256m -Xms128m
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - hello-network

networks:
  hello-network:
    driver: bridge

